mutation WebsiteRequestCreate($input: RequestCreateInput!) {
  requestCreate(input: $input) {
    request {
      id
      title
      createdAt
    }
    userErrors {
      message
      path
    }
  }
}
